import { LocationSwaggerEntity } from '..';

describe('LocationSwaggerEntity', () => {
  it('should create an instance of LocationSwaggerEntity with the correct values', () => {
    // Arrange and Act
    const locationSwaggerEntity = new LocationSwaggerEntity();

    // Assert
    expect(locationSwaggerEntity).toBeInstanceOf(LocationSwaggerEntity);
  });
});
