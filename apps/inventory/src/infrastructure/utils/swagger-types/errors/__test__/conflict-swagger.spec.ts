import { ConflictSwagger } from '..';

describe('ConflictSwagger', () => {
  it('should create an instance of ConflictSwagger with the correct values', () => {
    // Arrange and Act
    const conflictSwagger = new ConflictSwagger();

    // Assert
    expect(conflictSwagger).toBeInstanceOf(ConflictSwagger);
  });
});
