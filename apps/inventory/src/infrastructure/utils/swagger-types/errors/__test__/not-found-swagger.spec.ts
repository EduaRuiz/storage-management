import { NotFoundSwagger } from '..';

describe('NotFoundSwagger', () => {
  it('should create an instance of NotFoundSwagger with the correct values', () => {
    // Arrange and Act
    const notFoundSwagger = new NotFoundSwagger();

    // Assert
    expect(notFoundSwagger).toBeInstanceOf(NotFoundSwagger);
  });
});
